:ruby
  @menu   = :version, :edit, :history, :new
  @title  = @page.pretty_name
  @footer = "Committed by #{@page.commit.committer.name}, #{date @page.commit.committer_date}"
  @feed = (@page.path/'changelog.rss').urlpath
~ @content
